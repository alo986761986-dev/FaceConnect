# Render Blueprint - Infrastructure as Code
# Deploy with: Connect GitHub repo to Render and it auto-detects this file

services:
  # Backend API Service
  - type: web
    name: faceconnect-api
    runtime: python
    region: oregon
    plan: free
    branch: main
    rootDir: backend
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
      - key: MONGO_URL
        sync: false  # Set manually in Render dashboard
      - key: DB_NAME
        value: faceconnect
      - key: JWT_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        value: "*"
    healthCheckPath: /api/health
    autoDeploy: true

  # Frontend Static Site (Optional - if you want to host frontend too)
  # Uncomment if needed
  # - type: web
  #   name: faceconnect-web
  #   runtime: static
  #   branch: main
  #   rootDir: frontend
  #   buildCommand: yarn install && yarn build
  #   staticPublishPath: build
  #   headers:
  #     - path: /*
  #       name: Cache-Control
  #       value: public, max-age=0, must-revalidate
  #   routes:
  #     - type: rewrite
  #       source: /*
  #       destination: /index.html
